class feedback_input:
    def analyze(self, feedback_text):
        # GPT API를 통해 피드백 분석 + GPT 작동 안 할 시 예시로 단순 처리
        feedback_type = "color"  # 피드백 예시: "밝은 색상"
        feedback_value = "bright"  # 예시 피드백값

        return feedback_type, feedback_value
